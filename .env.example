# Optional. Leave unset in development: the app derives the phone-server address
# from the Metro dev server host, so a real device on the same Wi-Fi just works.
# Set this when pointing at a deployed API.
# EXPO_PUBLIC_API_URL=https://your-api.example.com
